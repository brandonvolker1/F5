---
- name: Create a VIP, pool, pool members, and nodes
  hosts: 192.168.2.101
  connection: local
  
  tasks:
      - name: Create node
        bigip_node:
            host: "{{ item.host }}"
            name: "{{ item.nodename }}"
            password: admin
            server: 192.168.2.101
            user: admin
            validate_certs: no
        delegate_to: localhost
        with_items:
            - host: 29.37.28.38
              nodename: ProdNode_101
